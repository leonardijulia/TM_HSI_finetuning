_target_: lightning.pytorch.trainer.Trainer

accelerator: auto
devices: 1

precision: 16-mixed

log_every_n_steps: 5
default_root_dir: ${project_root}

min_epochs: 1
max_epochs: 100

deterministic: False